# gstack is installed into each teammate's home dir (~/.claude/skills/gstack)
# by scripts/bootstrap.sh — nothing to ignore inside the repo for that.

# Node / Bun
node_modules/
.bun/
*.log

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Claude Code / Codex per-user state (skill links, local settings)
.claude/settings.local.json
.codex/
